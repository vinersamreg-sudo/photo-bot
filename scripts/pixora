#!/usr/bin/env bash
set -euo pipefail
ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
PYTHON=${PIXORA_PYTHON:-"$ROOT/venv/bin/python"}
if [ ! -x "$PYTHON" ]; then
  PYTHON=${PIXORA_PYTHON:-python}
fi
exec "$PYTHON" -m app.content_studio.cli "$@"
